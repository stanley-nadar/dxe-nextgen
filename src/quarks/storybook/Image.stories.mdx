import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Image from 'quarks/Image';
import { image } from 'quarks/storybook/Image.stories';

<Meta
  title="Quarks/Image"
  component={Image}
  argTypes={{
    src: {
      control: { type: 'text' },
    },
    height: {
      control: { type: 'number' },
    },
    width: {
      control: { type: 'number' },
    },
    opacity: {
      control: { type: 'number', min: 0, max: 1, step: 0.1 },
    },
    objectPosition: {
      control: { type: 'text' },
    },
  }}
/>

# Image

The Image quark is an `<img>` tag with all the basic props found on
[Container](?path=/docs/quarks-container--container).

Below are some examples of basic props typically used with Image.

<Canvas>
  <Story name="Image" story={image} />
</Canvas>

<ArgsTable story="Image" />
