import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import { colorListArray } from 'atoms/colors/colors';

import Badge, { badgeSizeOptions } from 'molecules/Badge/Badge';
import * as stories from 'molecules/Badge/storybook/Badge.stories.tsx';

import { iconMap } from 'utils/storyUtils.stories.tsx';

<Meta
  title="Molecules/Badge"
  component={Badge}
  argTypes={{
    size: {
      options: Object.keys(badgeSizeOptions),
      control: { type: 'radio' },
    },
    backgroundColor: {
      description: 'Choose a color by referencing the values in color atom',
      options: [...colorListArray, undefined],
      control: { type: 'select' },
    },
    textColor: {
      description: 'Choose a color by referencing the values in color atom',
      options: [...colorListArray, undefined],
      control: { type: 'select' },
    },
    startIcon: {
      options: Object.keys(iconMap),
      mapping: iconMap,
      control: { type: 'select' },
    },
    endIcon: {
      options: Object.keys(iconMap),
      mapping: iconMap,
      control: { type: 'select' },
    },
  }}
/>

# Badges

Badges help highlight important information, such as notifications or new and unread messages. They are primarily used
for communicating secondary or additional information to text.

<Canvas>
  <Story name="Normal" story={stories.normalBadge} />
</Canvas>

### Props

<ArgsTable story="Normal" />
